<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybaits.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.bijf.dao.IUserDao">
    <!-- 配置查询所有 -->
    <select id="findAll" resultType="pers.bijf.domain.User">
        select * from user;
    </select>
</mapper>